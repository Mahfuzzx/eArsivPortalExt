        let userIDElement = document.querySelector('div[rel="userID"] p');

        if (userIDElement) {
            if (userIDElement.innerText == "54055438486" && notElement.value.indexOf("BANKA HESAP BİLGİLERİMİZ:") == -1) {
                notElement.value += `

BANKA HESAP BİLGİLERİMİZ:
ALBARAKA TÜRK KATILIM BANKASI A.Ş.
MALTEPE ŞUBESİ
İSMAİL AY
IBAN: TR92 0020 3000 0028 1110 0000 01`;
            }
        }
    // if edit note button clicked:
    // - add a transparent modal layer at the top of all elements, create a little window in it.
    // - create a label "Başlık:" in the window.
    // - create an input below "Başlık" label.
    // - create a label "Kullanıcı Notu:".
    // - create a text area below "Kullanıcı Notu" label.
    // - add a "Kaydet" button at the right corder.
    // - the button saves title and note with userID.

i want to finish loadUserNote function.
my goal is to allow user to edit their own note and save then the code automatically updates note.